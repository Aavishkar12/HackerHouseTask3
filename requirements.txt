# Stage 1: face detection & encoding (DeepFace / ArcFace + RetinaFace)
#
# NOTE: deepface pulls in TensorFlow (~600MB) and downloads ~335MB of
# model weights to ~/.deepface on first run. Expect the first encode to
# take ~15s while weights download; subsequent runs are ~3.7s per image.
deepface==0.0.100
tf-keras==2.21.0
tensorflow==2.21.0
retina-face==0.0.18

numpy==2.4.6
Pillow==12.3.0
opencv-python==5.0.0.93

# Stage 2: reverse image search via browser automation
# After installing, you must also download the browser binary:
#     playwright install chromium
playwright==1.62.0
requests==2.34.2

# Stage 3: blockchain anchoring & verification
#
# web3 talks JSON-RPC to any chain: a local Ganache/Anvil/Hardhat node,
# or a public testnet (Sepolia, Polygon Amoy).
#
# The [tester] extra adds an in-process EVM (eth-tester + py-evm) so
# Stage 3 can run with NO node installed at all — used by
# `anchor_record.py --simulated` and by tests/test_chain.py.
#
# No Solidity compiler is needed: contracts/MatchRegistry.json ships
# precompiled. Only run scripts/compile_contract.py if you edit the .sol.
web3==8.0.0
